name: Redaction & Repo Hygiene

on:
  pull_request:
    branches: [ main, master, develop ]
  push:
    branches: [ main, master, develop ]
  workflow_dispatch:

jobs:
  check:
    name: Security & Privacy Checks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for better analysis
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r lichen-protocol-mvp/requirements.txt
          # Optional: install dev requirements and additional tools
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
          pip install pytest watchdog
      
      - name: Run redaction unit tests
        run: |
          echo "🧪 Running redaction and observability tests..."
          pytest -q eval/tests/ lichen-protocol-mvp/hallway/tests/ -k "redaction or observability" --tb=short      
      - name: Ensure no logs or indexes in tree
        run: |
          echo "🔍 Checking for tracked logs and indexes..."
          if git ls-files | grep -E '(^|/)logs/|\.jsonl$|lichen-chunker/index/'; then
            echo "❌ Found tracked logs or indexes in repository"
            echo "These files should be excluded by .gitignore:"
            git ls-files | grep -E '(^|/)logs/|\.jsonl$|lichen-chunker/index/'
            exit 1
          else
            echo "✅ No logs or indexes found in tracked files"
          fi
      
      - name: Ensure large/binary files not tracked
        run: |
          echo "🔍 Checking for large or binary files..."
          large_files=()
          binary_files=()
          
          # Check file sizes
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              size=$(wc -c <"$file")
              if [ $size -gt 1000000 ]; then
                large_files+=("$file ($(numfmt --to=iec $size))")
              fi
              
              # Check for binary files
              if file "$file" | grep -q "binary\|executable\|compressed\|image\|audio\|video"; then
                binary_files+=("$file")
              fi
            fi
          done < <(git ls-files)
          
          if [ ${#large_files[@]} -gt 0 ]; then
            echo "❌ Found large files in repository:"
            printf '%s\n' "${large_files[@]}"
            exit 1
          fi
          
          if [ ${#binary_files[@]} -gt 0 ]; then
            echo "❌ Found binary files in repository:"
            printf '%s\n' "${binary_files[@]}"
            exit 1
          fi
          
          echo "✅ No large or binary files found in tracked files"
      
      - name: Check .gitignore coverage
        shell: bash
        run: |
          set -euo pipefail
          echo "🔍 Verifying .gitignore coverage (semantic check)..."

          if [ ! -f .gitignore ]; then
            echo "❌ .gitignore file not found"
            exit 1
          fi

          # Sample paths that MUST be ignored by .gitignore behavior
          required_paths=(
            "logs/example.log"
            "somewhere/file.jsonl"
            "lichen-chunker/index/dump.faiss"
            "models/example.pkl"
            "datasets/part.parquet"
            ".venv/bin/python"
            "pkg/.venv/bin/python"
            ".DS_Store"
          )

          missing=()
          for p in "${required_paths[@]}"; do
            if ! git check-ignore -q "$p"; then
              missing+=("$p")
            fi
          done

          if ((${#missing[@]})); then
            echo "❌ These example paths are NOT ignored by your current .gitignore:"
            printf '  - %s\n' "${missing[@]}"
            echo "Hint: add patterns like these if you don't have them yet:"
            cat <<'PATTERNS'
    logs/
    *.jsonl
    lichen-chunker/index/
    *.faiss
    *.pkl
    *.parquet
    .venv/
    **/.venv/
    .DS_Store
    PATTERNS
            exit 1
          fi

          echo "✅ .gitignore correctly ignores required paths"
      
      - name: Check pre-commit hook
        run: |
          echo "🔍 Verifying pre-commit hook..."
          
          if [ ! -f .git/hooks/pre-commit ]; then
            echo "❌ Pre-commit hook not found"
            exit 1
          fi
          
          if [ ! -x .git/hooks/pre-commit ]; then
            echo "❌ Pre-commit hook not executable"
            exit 1
          fi
          
          echo "✅ Pre-commit hook exists and is executable"
      
      - name: Test redaction functionality
        run: |
          echo "🧪 Testing redaction functionality..."
          
          # Test with redaction enabled
          export REDACT_LOGS=1
          python3 -c "
          import sys
          sys.path.insert(0, 'lichen-protocol-mvp/hallway')
          from redaction import redact_text
          result = redact_text('Contact john@example.com for details')
          assert '[EMAIL_REDACTED]' in result
          assert 'john@example.com' not in result
          print('✅ Redaction enabled test passed')
          "
          
          # Test with redaction disabled
          export REDACT_LOGS=0
          python3 -c "
          import sys
          sys.path.insert(0, 'lichen-protocol-mvp/hallway')
          from redaction import redact_text
          result = redact_text('Contact john@example.com for details')
          assert 'john@example.com' in result
          assert '[EMAIL_REDACTED]' not in result
          print('✅ Redaction disabled test passed')
          "
      
      - name: Security summary
        run: |
          echo "🎉 All security and privacy checks passed!"
          echo "✅ Redaction tests passed"
          echo "✅ No sensitive files in repository"
          echo "✅ No large or binary files tracked"
          echo "✅ .gitignore properly configured"
          echo "✅ Pre-commit hook functional"
          echo "✅ Redaction system working correctly"
